<!DOCTYPE html>
<html lang="<%= lang || 'en' %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>To-Do App</title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>
  <header>
    <h1>To-Do App</h1>
    <form id="langForm" style="display:inline;">
      <select id="langSelect" name="lang">
        <option value="en" <%= lang === 'en' ? 'selected' : '' %>>English</option>
        <option value="es" <%= lang === 'es' ? 'selected' : '' %>>Español</option>
        <option value="hu" <%= lang === 'hu' ? 'selected' : '' %>>Magyar</option>
      </select>
    </form>
  </header>
  <main>
    <div id="popupForm">
      <form action="/add" method="POST">
        <h3><%= t.newTask %></h3>
        <input type="text" name="title" placeholder="<%= t.taskTitle %>" required>
        <textarea name="description" placeholder="<%= t.taskDescription %>"></textarea>
        <label for="todoStatus"><strong><%= t.status %>:</strong></label>
        <select name="status">
          <option value="Pending"><%= t.pending %></option>
          <option value="Completed"><%= t.completed %></option>
        </select>
        <label for="todoPriority"><strong><%= t.priority %>:</strong></label>
        <select name="priority">
          <option value="Low"><%= t.low %></option>
          <option value="Normal" selected><%= t.normal %></option>
          <option value="High"><%= t.high %></option>
        </select>
        <button type="submit"><%= t.addTask %></button>
        <a href="/tasks" class="buttonStyle"><%= t.cancel %></a>
      </form>
    </div>
  </main>
  <footer>
    <p>&copy; 2025 To-Do App by Carlitos & Copilot</p>
  </footer>
  <script>
    document.getElementById('langSelect').addEventListener('change', function() {
      const url = new URL(window.location.href);
      url.searchParams.set('lang', this.value);
      window.location.href = url.toString();
    });
  </script>
</body>
</html>