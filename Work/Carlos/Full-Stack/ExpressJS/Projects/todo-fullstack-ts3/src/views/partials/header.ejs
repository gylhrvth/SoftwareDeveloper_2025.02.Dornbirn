<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= locals.title || "To-Do App" %></title>
  <link rel="stylesheet" href="/main.css">
  <link rel="shortcut icon" href="//cdn2.auth0.com/styleguide/latest/lib/logos/img/favicon.png">
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <header>
    <div class="header-container">
      <div class="header-left">
        <h1>To-Do App</h1>
      </div>
      <div class="header-right">
        <div class="userLogic">
          <span class="rightHeaderText">
            <%= t.user %>:
            <% if (user) { %>
              <a href="http://localhost:3003/profile">
                <%= (user.name && user.name.trim()) ? user.name
                  : ((user.nickname && user.nickname.trim()) ? user.nickname : 'Unnamed') %>
              </a>
            <% } else { %>
              <span class="user-guest">Guest</span>
            <% } %>
          </span>
          <% if (locals.user) { %>
            <span><a class="logout" href="/logout">Logout</a></span>
          <% } else { %>
            <span><a class="logout" href="/login">Login</a></span>
          <% } %>
        </div>
      </div>
    </div>
    <div class="lowerHeader">
      <div class="lowerHeaderLeft">
        <a href="/" class="nav-link">Home</a>
        <a href="/tasks" class="nav-link">Tasks</a>
        <a href="/profile" class="nav-link">Profile</a>
        <% if (user && user.name === 'ScraleGi') { %>
        <a class="nav-link" href="/tasks?all=1">Admin</a>
        <% } %>
      </div>
      <div class="lowerHeaderRight">
        <a href="https://github.com/auth0/express-openid-connect" target="_blank" class="nav-link">GitHub</a>
      </div>
    </div>
  </header>
  <main>