<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Formulardaten (Admin)</title>
  <style>
    body { font-family: sans-serif; padding: 2rem; }
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 8px; }
    th { background-color: #f2f2f2; }
  </style>
</head>
<body>
  <h1>Formulardaten (nur Entwickler)</h1>

  <% if (daten.length === 0) { %>
    <p>Es wurden noch keine Daten übermittelt.</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <% Object.keys(daten[0]).forEach(key => { %>
            <th><%= key %></th>
          <% }) %>
        </tr>
      </thead>
      <tbody>
        <% daten.forEach(eintrag => { %>
          <tr>
            <% Object.values(eintrag).forEach(wert => { %>
              <td><%= wert %></td>
            <% }) %>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
</body>
</html>
