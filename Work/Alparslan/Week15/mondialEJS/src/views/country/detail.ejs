<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= country.name %> - Details</title>
    <style>
    .custom-button {
        background-color: #1976d2;
        color: #fff;
        border: none;
        padding: 0.5em 1.2em;
        border-radius: 4px;
        cursor: pointer;
        font-size: 1.3em;
        transition: background 0.2s;
        text-decoration: none;
    }
    button:hover {
        background-color: #1565c0;
    }
    </style>
</head>
<body>
    <h1><%= country.Name -%></h1>
    <a href="/country/<%= country.Code -%>/edit" class="custom-button">Edit</a>
    <p><strong>Code:</strong> <%= country.Code -%></p>
    <p><strong>Capital:</strong> <%= country.Capital -%></p>
    <p><strong>Province:</strong> <%= country.Province -%></p>
    <p><strong>Population:</strong> <%= country.Population.toLocaleString('de-AT') %></p>
    <p><strong>Area:</strong> <%= country.Area.toLocaleString('de-AT') %> km²</p>
    <a href="/country">Back to Country List</a>
</body>
</html>