<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <title>Städteverwaltung</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <main>
    <h1>Städteverwaltung</h1>

    <form action="/add" method="POST" id="city-form">
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" required placeholder="Stadtname" />

      <label for="population">Bevölkerung:</label>
      <input type="number" id="population" name="population" required min="1" placeholder="Einwohnerzahl" />

      <button type="submit">Hinzufügen</button>
    </form>

    <h2>Städte Liste</h2>

    <% if (cities.length === 0) { %>
      <p>Keine Städte vorhanden.</p>
    <% } else { %>
      <ul>
        <% cities.forEach(city => { %>
          <li>
            <strong><%= city.name %></strong> — <%= city.population %> Einwohner
            <form action="/delete/<%= city.id %>" method="POST" style="display:inline;">
              <button type="submit" class="delete-btn">Löschen</button>
            </form>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </main>
</body>
</html